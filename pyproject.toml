[project]
name = "ai-agent"
version = "0.1.0"
description = "AI Agent build with Boot.dev course"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai==2.44.0",
    "python-dotenv==1.1.0",
]
